---
 - hosts: localhost
   user: ansible
   become: yes
   connection: ssh

   tasks:
     - name: install maven
       action: yum name=maven state=present

     - name: install java
       action: yum name=java state=present
